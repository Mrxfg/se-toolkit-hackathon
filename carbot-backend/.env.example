# Backend Environment Variables
#
# NOTE: When using Docker, these values are set in docker-compose.yml
# This file is only needed for local development without Docker

DB_NAME=carbot
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432
